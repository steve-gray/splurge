'use strict'

/**
 * The Workflow class contains the various tasks we are about to execute
 * and combines them for runtime execution.
 */
class Workflow {
  /**
   * Initialize a new instance of the workflow.
   */
  constructor() {
    
  }
}

module.exports = Workflow;
